import { createUseStyles } from "react-jss";

const styles = createUseStyles({
  main: {
    minWidth: 320,
    maxWidth: 1200,
    width: "100vw",

    margin: "0 auto",
  },
});

export default styles;
