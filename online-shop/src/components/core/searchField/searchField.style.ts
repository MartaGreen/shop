import { createUseStyles } from "react-jss";

const styles = createUseStyles({
  input: {
    width: "90%",
    height: 30,
    borderRadius: 30,
    padding: "5px 15px",
    border: "none",
    outline: "none",
  },
});

export default styles;
